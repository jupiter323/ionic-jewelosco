import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { ContracttermsPage } from './contractterms';

@NgModule({
  declarations: [
    ContracttermsPage,
  ],
  imports: [
    IonicPageModule.forChild(ContracttermsPage),
  ],
})
export class ContracttermsPageModule {}
