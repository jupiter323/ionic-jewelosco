import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { CustommodalPage } from './custommodal';

@NgModule({
  declarations: [
    CustommodalPage,
  ],
  imports: [
    IonicPageModule.forChild(CustommodalPage),
  ],
})
export class CustommodalPageModule {}
